import React from'react';import{render,screen}from'@testing-library/react';import{MemoryRouter,Route,Routes}from'react-router-dom';import TripLayout from'./TripLayout';import{getTrip}from'../features/trips/api/tripsApi';
jest.mock('../features/trips/api/tripsApi',()=>({getTrip:jest.fn()}));jest.mock('../components/Layout/MainLayout',()=>({children})=><>{children}</>);
test('renders trip navigation and the nested outlet',async()=>{getTrip.mockResolvedValue({id:'t1',title:'Outlet Trip',budget:'100',currency:'SAR',join_code:'ABC',lifecycle_status:'active',current_member:{id:'m1',role:'owner'}});render(<MemoryRouter initialEntries={['/trips/t1/overview']}><Routes><Route path="/trips/:tripId" element={<TripLayout/>}><Route path="overview" element={<p>overview outlet</p>}/></Route></Routes></MemoryRouter>);expect(await screen.findByText('Outlet Trip')).toBeInTheDocument();expect(screen.getByText('overview outlet')).toBeInTheDocument();expect(screen.getByRole('navigation')).toBeInTheDocument()});
