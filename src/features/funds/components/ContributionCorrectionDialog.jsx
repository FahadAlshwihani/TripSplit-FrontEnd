import React,{useState}from'react';import{useTranslation}from'react-i18next';
export default function ContributionCorrectionDialog({row,currency,onSave,onClose}){const{t}=useTranslation(),[amount,setAmount]=useState(row.amount),[reason,setReason]=useState('');return<div className="fund-correction" role="dialog" aria-modal="true"><h3>{t('fund.correctContribution')}</h3><p>{row.display_name}: {row.amount} {currency}</p><label>{t('fund.correctAmount')}<input inputMode="decimal" type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)}/></label><label>{t('fund.correctionReason')}<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><div className="fund-actions"><button type="button" disabled={!reason||!amount} onClick={()=>onSave(row,{amount,reason})}>{t('common.save')}</button><button type="button" onClick={onClose}>{t('common.cancel')}</button></div></div>}
