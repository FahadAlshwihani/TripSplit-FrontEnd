import React from'react';import{useTranslation}from'react-i18next';import Avatar from'../../profile/components/Avatar';import{avatarKeyFromAvatar}from'../../profile/utils/avatarKey';
export default function FundOverview({fund,currency,activeMembers,canManage,onUpdate}){const{t}=useTranslation(),a=fund.accounting,deficit=Number(a.deficit||0);return<><header className="fund-header"><div><h2>{fund.name}</h2><p>{t('fund.holder')}: <Avatar avatarKey={avatarKeyFromAvatar(fund.holder.avatar)} displayName={fund.holder.display_name} size="sm" /> {fund.holder.display_name}</p></div>{canManage&&<select aria-label={t('fund.changeHolder')} className="pc-input compact-input" value={fund.holder.id} onChange={e=>onUpdate({holder_id:e.target.value})}>{activeMembers.map(m=><option key={m.id} value={m.id}>{m.display_name}</option>)}</select>}</header><div className="fund-metrics"><div><span>{t('fund.collected')}</span><strong>{a.collected} {currency}</strong></div><div><span>{t('fund.spent')}</span><strong>{a.spent} {currency}</strong></div><div><span>{t('fund.refunded')}</span><strong>{a.refunded} {currency}</strong></div><div><span>{deficit?t('fund.deficit'):t('fund.available')}</span><strong>{deficit?a.deficit:a.balance} {currency}</strong></div></div></>}
