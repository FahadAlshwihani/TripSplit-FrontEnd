import React,{useState}from'react';import{useTranslation}from'react-i18next';const today=()=>new Date().toISOString().slice(0,10);
export default function ContributionForm({rounds,members,onSubmit}){const{t}=useTranslation(),[form,setForm]=useState({round_id:'',member_id:'',amount:'',contribution_date:today(),note:''});return<form className="fund-form" onSubmit={e=>{e.preventDefault();onSubmit(form.round_id,{...form,idempotency_key:crypto.randomUUID()})}}><h3>{t('fund.recordContribution')}</h3><select aria-label={t('fund.round')} className="pc-input" value={form.round_id} onChange={e=>setForm({...form,round_id:e.target.value})} required><option value="">—</option>{rounds.filter(r=>r.status==='open').map(r=><option key={r.id} value={r.id}>{r.title}</option>)}</select><select aria-label={t('fund.member')} className="pc-input" value={form.member_id} onChange={e=>setForm({...form,member_id:e.target.value})} required><option value="">—</option>{members.map(m=><option key={m.id} value={m.id}>{m.display_name}</option>)}</select><input aria-label={t('fund.amount')} className="pc-input" inputMode="decimal" type="number" min="0.01" step="0.01" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} required/><input aria-label={t('expense.date')} className="pc-input" type="date" value={form.contribution_date} onChange={e=>setForm({...form,contribution_date:e.target.value})}/><button>{t('fund.record')}</button></form>}
