import React from'react';import{render,screen}from'@testing-library/react';import{MemoryRouter,Outlet,Route,Routes}from'react-router-dom';import TripOverviewPage from'../pages/TripOverviewPage';import TripSettingsPage from'../pages/TripSettingsPage';import ExpensesPage from'../../expenses/pages/ExpensesPage';import{getFund}from'../../funds/api/fundsApi';import{getActivity}from'../../activity/api/activityApi';import{getExpenses}from'../../expenses/api/expensesApi';import{getTripSummary}from'../api/tripsApi';
jest.mock('../../../components/CustomChart',()=>()=>null);jest.mock('../../funds/api/fundsApi',()=>({getFund:jest.fn()}));jest.mock('../../activity/api/activityApi',()=>({getActivity:jest.fn()}));jest.mock('../../expenses/api/expensesApi',()=>({getExpenses:jest.fn()}));jest.mock('../../categories/api/categoriesApi',()=>({getCategories:jest.fn()}));jest.mock('../../members/api/membersApi',()=>({getMembers:jest.fn()}));jest.mock('../../balances/api/balancesApi',()=>({getBalances:jest.fn()}));jest.mock('../api/tripsApi',()=>({getTripSummary:jest.fn()}));
const ctx={trip:{id:'t',title:'T',budget:'100',currency:'SAR',lifecycle_status:'active'},tripId:'t',currentMember:{id:'m',role:'owner'},permissions:{canCreateExpense:false,canArchiveTrip:false,canEditExpense:()=>false}};const Shell=()=> <Outlet context={ctx}/>;const show=(path,element)=>render(<MemoryRouter initialEntries={[path]}><Routes><Route path="/trips/:tripId" element={<Shell/>}><Route path={path.split('/').pop()} element={element}/></Route></Routes></MemoryRouter>);
beforeEach(()=>jest.clearAllMocks());
test('settings does not request fund or expenses',()=>{show('/trips/t/settings',<TripSettingsPage/>);expect(getFund).not.toHaveBeenCalled();expect(getExpenses).not.toHaveBeenCalled()});
test('expenses does not request activity',async()=>{getExpenses.mockResolvedValue({results:[]});require('../../categories/api/categoriesApi').getCategories.mockResolvedValue({results:[]});require('../../members/api/membersApi').getMembers.mockResolvedValue({results:[]});getFund.mockResolvedValue(null);show('/trips/t/expenses',<ExpensesPage/>);expect(await screen.findByText(/expense.empty/i)).toBeInTheDocument();expect(getActivity).not.toHaveBeenCalled()});
test('overview fetches only summary balances and fund',async()=>{getTripSummary.mockResolvedValue({shared_spending:'0',personal_spending:'0',current_member:{balance:'0'}});require('../../balances/api/balancesApi').getBalances.mockResolvedValue({});getFund.mockResolvedValue(null);show('/trips/t/overview',<TripOverviewPage/>);expect(await screen.findByText(/summary.shared:/)).toBeInTheDocument();expect(getExpenses).not.toHaveBeenCalled();expect(getActivity).not.toHaveBeenCalled()});
test('expenses aborts its stale list request when the route unmounts',async()=>{let signal;getExpenses.mockImplementation((_tripId,config)=>{signal=config.signal;return new Promise(()=>{})});const view=show('/trips/t/expenses',<ExpensesPage/>);await screen.findByText(/loading/i);view.unmount();expect(signal.aborted).toBe(true)});
