import React from 'react';
import { useTranslation } from 'react-i18next';
import Avatar from '../../profile/components/Avatar';
import { avatarKeyFromAvatar } from '../../profile/utils/avatarKey';
export default function BansSection({ members,bans,onKick,onBan,onUnban }) { const {t}=useTranslation(); return <section><h3>{t('governance.moderation')}</h3>{members.filter(m=>m.role!=='owner'&&m.active!==false).map(m=><div className="management-row" key={m.id}><span className="member-avatar"><Avatar avatarKey={avatarKeyFromAvatar(m.avatar)} displayName={m.display_name} size="sm" /> {m.display_name}</span><div className="row-actions"><button onClick={()=>onKick(m)}>{t('governance.kick')}</button><button onClick={()=>onBan(m,'24h')}>{t('governance.ban24h')}</button><button onClick={()=>onBan(m,'permanent')}>{t('governance.banPermanent')}</button></div></div>)}<h3>{t('governance.bans')}</h3>{bans.filter(b=>b.active).map(b=><div className="management-row" key={b.id}><span className="member-avatar">{b.member && <Avatar avatarKey={avatarKeyFromAvatar(b.member.avatar)} displayName={b.member.display_name} size="sm" />} {b.member?.display_name||t('activity.unknown')}<small>{b.expires_at?new Date(b.expires_at).toLocaleString():t('governance.permanent')}</small></span><button onClick={()=>onUnban(b)}>{t('governance.unban')}</button></div>)}</section>; }
